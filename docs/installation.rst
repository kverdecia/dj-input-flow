============
Installation
============

At the command line::

    $ easy_install dj-input-flow

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-input-flow
    $ pip install dj-input-flow
